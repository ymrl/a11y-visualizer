---
"@a11y-visualizer/browser-extension": patch
---

Complete the Korean (ko) translation and fix wording inconsistencies in the UI texts:

- Korean: translate all remaining untranslated messages (state texts such as `Checked` / `Expanded` / `Pressed`, list structure errors, `Nested interactive element`, etc.), unify terms with the Korean User's Guide (e.g. 라벨, 폼 컨트롤), and fix a broken phrase (연결되지 없음 → 연결되지 않음)
- Japanese: unify 表 → テーブル in tip labels, use フォームコントロール in the label association message, remove polite endings left in two messages, and fix a typo (それらまとめる → それらをまとめる)
- English: fix typos and inconsistencies (`Langage` → `Language`, `Settings has been` → `Settings have been`, stray apostrophes after `role`, capitalization of tip labels and `Speed`)
